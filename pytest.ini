[pytest]
testpaths = tests
python_files = test_*.py
python_classes = Test*
python_functions = test_*
addopts =
    -v
    --tb=short
    --strict-markers
    --cov=backend
    --cov-report=term-missing
markers =
    unit: 단위 테스트
    integration: 통합 테스트
    e2e: E2E 테스트
