# Docker 빌드용: requirements.txt 기반. gpt4all 제외 (Docker에서는 Ollama 컨테이너 사용).
# 로컬: requirements.txt 사용 시 gpt4all 선택 설치 가능 (CLI·레거시 스크립트용).
#
fastapi>=0.104.0
uvicorn[standard]>=0.24.0
jinja2>=3.1.2
qdrant-client>=1.7.0
sentence-transformers>=2.2.2
torch>=2.0.0
sqlalchemy>=2.0.0
psycopg2-binary>=2.9.9
alembic>=1.12.0
watchdog>=3.0.0
pypdf>=3.17.0
python-docx>=1.1.0
openpyxl>=3.1.0
python-pptx>=0.6.21
olefile>=0.46  # HWP 파일 지원 (Phase 9-4-1)
Pillow>=10.0.0
pytesseract>=0.3.10
openai>=1.0.0
anthropic>=0.39.0  # Phase 8-2-7 Task 실행 (Claude API)
pydantic>=2.5.0
python-multipart>=0.0.6
scikit-learn>=1.3.0
numpy>=1.24.0
tqdm>=4.66.0

# 보안 (Phase 9-1)
python-jose[cryptography]>=3.3.0  # JWT 토큰
passlib[bcrypt]>=1.7.4  # 비밀번호 해싱 (Phase 14-5)
slowapi>=0.1.9  # Rate Limiting
redis>=5.0.0  # Redis 클라이언트 (Rate Limit 분산 저장소, 캐시)
python-dotenv>=1.0.0  # 환경변수 로딩
