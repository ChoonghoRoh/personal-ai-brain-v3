# 📊 Knowledge Studio 사용 매뉴얼

## 개요

Knowledge Studio는 저장된 지식 구조를 탐색하고 관리하는 웹 인터페이스입니다. 라벨별로 지식 청크를 필터링하고, 청크 간의 관계를 확인하며, Reasoning을 시작할 수 있습니다.

**접속 경로**: `/knowledge`

---

## 주요 기능

### 1. 라벨 필터

왼쪽 사이드바에서 라벨을 선택하여 해당 라벨이 부여된 지식 청크만 필터링할 수 있습니다.

- **전체**: 모든 청크 표시
- **라벨 목록**: 등록된 모든 라벨이 표시됩니다
  - 라벨 이름과 타입(project_phase, role, domain, importance)이 함께 표시됩니다
  - 라벨을 클릭하면 해당 라벨이 부여된 청크만 표시됩니다

### 2. 청크 목록

선택한 라벨에 해당하는 지식 청크 목록이 카드 형태로 표시됩니다.

각 청크 카드에는 다음 정보가 포함됩니다:

- **메타 정보**

  - 프로젝트 이름 (있는 경우)
  - 문서 이름
  - 청크 인덱스 번호

- **청크 내용 미리보기**

  - 처음 200자까지 표시됩니다
  - 전체 내용은 청크 상세에서 확인할 수 있습니다

- **라벨 배지**

  - 부여된 라벨이 배지 형태로 표시됩니다
  - 라벨이 없으면 "라벨 없음"으로 표시됩니다

- **관계 정보**
  - 나가는 관계 수 (이 청크에서 시작하는 관계)
  - 들어오는 관계 수 (이 청크로 도착하는 관계)

### 3. 청크 상세 보기

청크 카드를 클릭하면 하단에 상세 정보가 표시됩니다.

#### 3.1 기본 정보

- 프로젝트 이름
- 문서 이름
- 청크 인덱스
- 전체 청크 내용

#### 3.2 라벨 정보

- 부여된 모든 라벨 목록
- 각 라벨의 이름과 타입 표시

#### 3.3 관계 정보

**나가는 관계**

- 이 청크에서 시작하는 모든 관계
- 관계 타입 (예: `cause-of`, `result-of`, `refers-to` 등)
- 대상 청크의 내용 미리보기
- 관계 설명 (있는 경우)

**들어오는 관계**

- 이 청크로 도착하는 모든 관계
- 출발 청크의 내용 미리보기
- 관계 타입 및 설명

#### 3.4 Reasoning 시작

"🧠 이 청크로 Reasoning 시작" 버튼을 클릭하면:

- Reasoning Lab (`/reason`) 페이지로 이동합니다
- 선택한 청크가 시드(seed) 청크로 설정됩니다
- 질문 필드에 청크 내용이 자동으로 입력됩니다

---

## 사용 시나리오

### 시나리오 1: 특정 라벨의 지식 확인

1. 왼쪽 사이드바에서 원하는 라벨을 클릭합니다
2. 해당 라벨이 부여된 모든 청크가 표시됩니다
3. 청크를 클릭하여 상세 내용을 확인합니다

### 시나리오 2: 청크 간 관계 추적

1. 청크를 클릭하여 상세 정보를 엽니다
2. "나가는 관계" 섹션에서 이 청크와 연결된 다른 청크를 확인합니다
3. 관계 타입을 통해 청크 간의 논리적 연결을 파악합니다

### 시나리오 3: Reasoning 시작

1. 관심 있는 청크를 선택합니다
2. 청크 상세에서 "🧠 이 청크로 Reasoning 시작" 버튼을 클릭합니다
3. Reasoning Lab에서 해당 청크를 기반으로 추론을 시작합니다

---

## URL 파라미터

### document_id

특정 문서의 청크만 필터링하려면 URL에 `document_id` 파라미터를 추가할 수 있습니다.

**예시**:

```
/knowledge?document_id=5
```

이렇게 하면 문서 ID가 5인 문서의 청크만 표시됩니다.

---

## API 엔드포인트

Knowledge Studio는 다음 API를 사용합니다:

- `GET /api/labels` - 라벨 목록 조회
- `GET /api/knowledge/chunks` - 청크 목록 조회
  - 쿼리 파라미터:
    - `label_id`: 특정 라벨로 필터링
    - `document_id`: 특정 문서로 필터링
- `GET /api/knowledge/chunks/{id}` - 청크 상세 조회

---

## 팁

1. **빠른 탐색**: 라벨을 선택하면 관련 청크만 표시되어 탐색이 빠릅니다
2. **관계 추적**: 청크 상세에서 관계를 확인하면 지식의 연결 구조를 이해할 수 있습니다
3. **Reasoning 연계**: 청크에서 바로 Reasoning을 시작하면 맥락이 유지된 추론이 가능합니다

---

## 문제 해결

### 청크가 표시되지 않을 때

- "전체" 라벨을 선택하여 모든 청크를 확인해보세요
- 다른 라벨을 선택해보세요
- 브라우저 콘솔에서 오류 메시지를 확인하세요

### 관계 정보가 없을 때

- 관계가 생성되지 않은 청크일 수 있습니다
- Knowledge Admin에서 관계를 생성할 수 있습니다 (향후 기능)

### 페이지가 로드되지 않을 때

- 서버가 실행 중인지 확인하세요
- 네트워크 연결을 확인하세요
- 브라우저를 새로고침해보세요
