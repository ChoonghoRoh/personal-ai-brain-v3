---
doc_type: ai-rule
rule_domain: phase
rule_role: naming
version: 1.0
status: active
owner: ai
last_updated: 2026-01-28
---

# Phase Naming Rules

> 이 문서는 Phase를 **일관되게 명명·구분·판단**하기 위한 기준 문서이다.
> Phase 명명 규칙은 업무 자동화, AI 판단, 폴더 구조, Task ID 생성의 기준이 된다.

---

## 1. Phase의 정의

> **Phase란 하나의 명확한 목표를 가진 개발 단위이며,
> 완료 여부를 판단할 수 있고 반드시 종료되는 작업 묶음이다.**

Phase는 다음 성질을 가진다.

- 항상 시작과 종료가 존재한다
- 다음 Phase로 이어진다
- 완료된 Phase는 수정하지 않는다
- 실패하거나 되돌림이 필요하면 새로운 Phase를 만든다

---

## 2. Phase ID 기본 형식

```
Phase-X-Y-Z
```

| 항목 | 의미                          |
| ---- | ----------------------------- |
| X    | Major 단계 (시스템 성격 전환) |
| Y    | 목표·전략 단위                |
| Z    | 반복·보완 단계                |

AI는 **ID(X-Y-Z)** 로 판단하고,
사람은 **Phase Name**으로 이해한다.
Phase 판단 시 X → Y → Z 순으로 우선 적용한다.
개념 ID / 파일명 분리

---

## 3. Phase Name (사람용 이름)

Phase ID와 함께 **의미를 설명하는 이름을 반드시 붙인다.**

### 형식

```
Phase-X-Y-Z—<Human Readable Name>
```

### 예시

```
Phase-8-0-0—Search Performance Optimization
Phase-8-1-0—Automation Trigger Introduction
Phase-8-2-0—Stability & Error Handling
```

---

## 4. X (Major Phase) 규칙

> **시스템의 성격이 바뀌는 시점**에만 X를 증가시킨다.

### X 증가 조건

- 아키텍처 구조 변화
- 개발/운영 방식 전환
- 자동화 레벨의 질적 변화
- 프로젝트 목적 전환

### 예시

- Phase-7-x-x → Phase-8-x-x (UX 중심 → 자동화 중심)
- Phase-8-x-x → Phase-9-x-x (단일 시스템 → 멀티 에이전트)

📌 X는 자주 증가하지 않는다.

---

## 5. Y (목표·전략 Phase) 규칙

> **하나의 핵심 목표 또는 전략 묶음 단위**

### Y 증가 조건

- 핵심 목표 변경
- 작업 성격 변경 (성능 → 자동화 등)
- 동일 목표 내에서 전략이 달라질 때

### 예시

```
Phase-8-0-x—성능 개선
Phase-8-1-x—자동화 도입
Phase-8-2-x—안정화
```

📌 Y가 바뀌면 **새 Phase 폴더를 생성한다.**

---

## 6. Z (Iteration / 보완) 규칙

> **동일 목표 내 반복·보완 단계**

### Z 증가 조건

- 성능 튜닝
- 버그 수정
- 테스트 보강
- 리팩터링(목표 불변)

### 예시

```
Phase-8-0-0 → Phase-8-0-1 → Phase-8-0-2
```

📌 Z 변경 시에는 **폴더를 새로 만들지 않는다.**
📌 파일명으로만 구분한다.

---

## 7. Phase 폴더 생성 규칙

| Phase 변화 | 폴더 생성     |
| ---------- | ------------- |
| X 변경     | 필수          |
| Y 변경     | 필수          |
| Z 변경     | 생성하지 않음 |

### 권장 구조 (Option 1: 문서 종류별 저장 위치)

**프로젝트 기준 경로**: `docs/phases/`

```
docs/phases/                              ← Phase 루트 (선택: 마스터 플랜 등)
 ├─ phase8-master-plan.md                 ← Phase 전체 실행 계획 (해당 Phase 루트에 둘 때)
docs/phases/phase-X-Y/                    ← Phase(X-Y) 단위 폴더
 ├─ phaseX-Y-Z-plan.md                    ← Phase 설계 (Z=반복, 0,1,2…)
 ├─ phaseX-Y-Z-todo-list.md               ← Phase 할 일 목록
 ├─ phaseX-Y-Z-summary.md                 ← Phase 결과 요약
 ├─ phaseX-Y-Z-test-report.md             ← Phase 단위 테스트 증빙
 ├─ phaseX-Y-Z-change-report.md           ← Phase 단위 변경 이력
 └─ tasks/                                ← 해당 Phase에서 생성된 Task만
      ├─ phaseX-Y-N-task.md               ← Task 실행 계획 (N=1,2,3…, 권장)
      ├─ phaseX-Y-N-task-test-result.md   ← Task 테스트 결과 (권장)
      ├─ phaseX-Y-N-<topic>-change-report.md   ← Task 변경 이력 (변형, phase-8-0 실적)
      ├─ phaseX-Y-N-<topic>-test-report.md     ← Task 테스트 증빙 (변형, phase-8-0 실적)
      └─ …
```

- **phase-X-Y 폴더**: Phase 단위 문서(plan, todo-list, summary, test-report, change-report). Z는 반복·보완 단계로 파일명에 포함.
- **phase-X-Y/tasks/ 폴더**: phaseX-Y-Z-todo-list에서 유도된 Task 문서만 저장.
  - **권장 파일명**: `phaseX-Y-N-task.md`, `phaseX-Y-N-task-test-result.md` (N = todo 항목 순번).
  - **변형(phase-8-0 실적)**: `phaseX-Y-N-<topic>-change-report.md`, `phaseX-Y-N-<topic>-test-report.md` (topic = kebab-case 작업 주제). change-report = 변경 이력, test-report = 테스트 결과 구분.
- 상세 저장·구분 기준: `docs/ai/ai-rule-decision.md` §2, `docs/phase-document-taxonomy.md` 참고.

---

## 8. 금지 규칙 (엄격)

- 완료된 Phase 수정 금지
- Phase ID 재사용 금지
- 하나의 Task가 여러 Phase에 걸치는 것 금지
- Phase 없이 Task 생성 금지

---

## 9. AI 행동 지침

- AI는 Phase 명명 규칙을 변경하지 않는다.
- AI는 다음 Phase ID와 Phase Name을 **제안만 한다.**
- 최종 생성은 사람 승인 또는 자동화 트리거로 수행한다.

---

## 10. 문서 위치 (고정)

```
/docs/ai/ai-rule-phase-naming.md
```

---

## 한 줄 요약

**Phase 명명 규칙은 업무 자동화의 기준 좌표이며, 모든 판단은 이 규칙을 기준으로 이루어진다.**
