---
doc_type: ai-rule
rule_domain: phase
rule_role: auto-generation
version: 1.0
status: active
owner: ai
last_updated: 2026-01-28
---

# Phase Auto Generation Rules

> 이 문서는 **Phase / Task를 자동으로 생성·판단·연결하기 위한 기준 문서**이다.
> 모든 업무 자동화 및 AI 판단은 본 규칙을 기준으로 동작한다.
> 본 문서에서 '생성'이란 실제 파일 생성을 의미하지 않으며, AI 또는 시스템의 생성 제안을 의미한다.

---

## 1. Phase 자동 생성의 기본 원칙

Phase는 다음 조건 중 **하나라도 충족되면 새로 생성**된다.

1. 핵심 목표가 변경될 때
2. 실행 전략 묶음이 변경될 때
3. 기존 Phase 범위 내에서 Done Definition 충족이 불가능할 때
4. Phase 종료 조건을 만족했을 때

---

## 2. Phase 종류(Type) 정의

Phase는 성격에 따라 Type을 가진다.
Type은 AI의 다음 Phase 방향 판단을 돕기 위한 메타 정보이다.

| Type      | 의미                    |
| --------- | ----------------------- |
| BUILD     | 신규 기능·시스템 구축   |
| IMPROVE   | 성능·UX·구조 개선       |
| STABILIZE | 안정화·버그 수정·테스트 |
| AUTOMATE  | 자동화 도입             |
| REFACTOR  | 구조 정리               |
| RESEARCH  | 조사·실험               |

> Phase Type은 **파일명에 포함하지 않으며**, plan 문서 내 메타 정보로만 관리한다.

---

## 3. Phase ID 규칙

### 형식

```
Phase-X-Y-Z
```

※ 파일명·폴더명에서는 dot(.)을 사용하지 않으며
phase-X-Y-Z 형식으로 변환한다.

| 항목 | 의미                          |
| ---- | ----------------------------- |
| X    | Major 단계 (시스템 성격 전환) |
| Y    | 목표·전략 단위                |
| Z    | 반복·보완 단계                |

---

## 4. Phase ID 자동 증가 규칙

### 4-1. Z 증가 (Iteration / 보완)

```
IF
- 목표 동일
- 전략 동일
- 성능 개선, 버그 수정, 튜닝 작업
THEN
- Z = Z + 1
```

예시:

```
Phase-8-0-0 → Phase-8-0-1
```

---

### 4-2. Y 증가 (전략·목표 변경)

```
IF
- 동일 Major(X)
- 핵심 목표 또는 전략 묶음 변경
THEN
- Y = Y + 1
- Z = 0
```

예시:

```
Phase-8-0-2 → Phase-8-1-0
```

(파일/폴더명: phase-8-0-2 → phase-8-1-0)
📁 **새 Phase 폴더 생성 (필수)**

---

### 4-3. X 증가 (Major 전환)

```
IF
- 시스템 아키텍처 변화
- 개발/운영 방식 전환
THEN
- X = X + 1
- Y = 0
- Z = 0
```

예시:

```
Phase-8-2-3 → Phase-9-0-0
```

📁 **완전히 새로운 Phase 폴더 트리 생성**

---

## 5. Phase 자동 생성 시나리오

### 시나리오 A. Phase 정상 종료

```
IF
- docs/phases/phase-X-Y/ 내 phaseX-Y-Z-summary.md 존재
- summary 문서는 다음 Phase 판단의 1차 입력값으로 사용된다
- todo 완료율 ≥ 90%
- High Priority task 100% 완료
THEN
- Phase-X-(Y+1)-0 plan 생성 제안
```

---

### 시나리오 B. Phase 한계 도달

```
IF
- Z 반복 ≥ 3 (권장 기준, 상황에 따라 조정 가능)
- 핵심 목표 미달
THEN
- Phase-X-(Y+1)-0 (IMPROVE or RESEARCH) 생성
```

---

### 시나리오 C. 자동화 전환 Phase

```
IF
- 동일 유형 작업 반복 ≥ 3회
- 규칙화 가능
THEN
- Phase-X-(Y+1)-0 (AUTOMATE) 생성
```

---

## 6. Phase 생성 시 기본 파일 세트

새 Phase가 생성되면 **아래 구조를 준수**한다. (저장 위치: `docs/phases/phase-X-Y/`, `docs/ai/ai-rule-decision.md` §2 참고)

```
docs/phases/phase-X-Y/
 ├─ phaseX-Y-0-plan.md          ← Phase 설계 (Z=0)
 ├─ phaseX-Y-0-summary.md       ← Phase 결과 요약 (생성 시점에는 비어 있음)
 ├─ phaseX-Y-0-todo-list.md     ← Phase 할 일 (별도 워크플로우·승인 후 생성 가능)
 └─ tasks/                      ← Task 문서 (todo-list에서 유도된 항목만)
      └─ (생성 규칙: §7 Task ID 규칙, §8 Task 자동 생성 규칙)
```

- summary 파일은 생성 시점에는 비어 있다. todo-list는 8-2-5 등 워크플로우·승인 후 생성된다.
- Phase 전체 실행 계획이 필요하면 `docs/phases/phaseX-master-plan.md` 등 Phase 루트에 둘 수 있다 (예: phase8-master-plan.md).

---

## 7. Task ID 규칙

### 형식 (개념 ID)

```
task-X-Y-N
```

| 항목 | 의미                                               |
| ---- | -------------------------------------------------- |
| X    | Major Phase                                        |
| Y    | Phase 전략 단위                                    |
| N    | Phase 내 순번(0,1,2,… 또는 1,2,3,… — Phase별 일관) |

### 파일명 (저장: docs/phases/phase-X-Y/tasks/)

**권장 (신규 Phase):**

```
phaseX-Y-N-task.md
phaseX-Y-N-task-test-result.md
```

**변형 (phase-8-0 실적 — Task별 변경·테스트 증빙 분리):**

```
phaseX-Y-N-<topic>-change-report.md   ← Task 변경 이력
phaseX-Y-N-<topic>-test-report.md     ← Task 테스트 결과
```

- topic = kebab-case 작업 주제 (예: search-performance-optimization).
- 예시: task-8-0-0 → `phase8-0-0-search-performance-optimization-change-report.md`, `phase8-0-0-search-performance-optimization-test-report.md`.

📌 **Task ID(N)는 동일 Phase 내에서 절대 재사용하지 않는다.**

---

## 8. Task 자동 생성 규칙

```
IF
- phaseX-Y-Z-todo-list.md (또는 phaseX-Y-0-todo-list.md) 에 신규 항목 존재
- 우선순위 조건 충족
THEN
- phase-X-Y/tasks/ 에 Task 문서 생성 제안
  - 권장: phaseX-Y-N-task.md (실행 계획)
  - 실행·테스트 후: phaseX-Y-N-task-test-result.md 또는
    phaseX-Y-N-<topic>-change-report.md, phaseX-Y-N-<topic>-test-report.md
```

- 생성 주체: Human 또는 AI/워크플로우. 저장 위치·구분 기준: `docs/ai/ai-rule-decision.md` §2.

---

## 9. Phase ↔ Task 연결 규칙

- 하나의 Task는 하나의 Phase에만 속한다.
- Phase 종료 후 미완료 Task는 이동하지 않는다.
- 다음 Phase에서는 **새 Task로 재정의**한다.

---

## 10. AI 행동 원칙

- AI는 Phase / Task를 **생성 제안만 수행**한다.
- 실제 생성·적용은 사람 승인 또는 자동화 트리거로 수행한다.
- AI는 본 문서의 규칙을 변경하지 않는다.

---

## 11. 문서 위치 (필수)

```
/docs/ai/ai-rule-phase-auto-generation.md
```

---

## 한 줄 요약

> \*\*이 문서는 업무 자동화의 기초 헌법
