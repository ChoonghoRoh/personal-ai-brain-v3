# Task 18-3-4: CSS 재구성

- **우선순위**: P1
- **의존성**: 18-3-1
- **담당 팀원**: frontend-dev
- **상태**: PENDING

## §1 목적
reason.css(607줄) + reason-sections.css(597줄) 핫스팟 해소.

## §2 변경 범위
- `web/public/css/reason.css` → `reason-base.css` + `reason-form.css` 분리
- `web/public/css/reason-sections.css` → `reason-results.css` + `reason-actions.css` 분리
- `web/public/css/reason-steps.css` (신규, Step UI 전용)
- `web/src/pages/reason.html` — link 태그 업데이트

## §3 분리 기준
```
reason-base.css (~200줄)      ← CSS 변수, 테마, 접근성, 다크모드
reason-form.css (~150줄)      ← 입력 폼, 모드 선택, form-group
reason-steps.css (~200줄)     ← Step 1~4 순차 진행 레이아웃
reason-results.css (~200줄)   ← 결과 표시, 시각화, 탭, 컨텍스트
reason-actions.css (~100줄)   ← 세션, 히스토리, 모달, 후속 작업
reason-advanced.css (유지)    ← Mermaid, 리스크 매트릭스
```

## §4 완료 기준
- [ ] 모든 분리 파일 500줄 이하
- [ ] 시각적 동일성 (기존 UI와 동일한 렌더링)
- [ ] reason.html link 태그 정상
