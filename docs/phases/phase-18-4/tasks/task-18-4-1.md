# Task 18-4-1: 시맨틱 검색 status 필터

- **우선순위**: P0
- **의존성**: 없음
- **담당 팀원**: backend-dev
- **상태**: PENDING

## §1 목적
Qdrant 검색 시 chunk status(approved/draft/rejected)로 필터링.

## §2 변경 범위
- `backend/services/search/hybrid_search.py` — semantic_search에 status 필터 추가
- `backend/services/search/search_service.py` — search에 status 파라미터 전달
- `backend/routers/search/search.py` — API에 status 쿼리 파라미터 추가

## §3 구현 상세
1. Qdrant payload filter 구성: `{"must": [{"key": "status", "match": {"value": "approved"}}]}`
2. status 파라미터 기본값: None (필터 안함), "approved", "draft", "rejected"
3. 기존 filters dict에 status 조건 추가
4. Qdrant payload에 status가 없는 경우 대비 (graceful fallback)

## §4 완료 기준
- [ ] status 필터 적용 검색 동작
- [ ] 기본값(None)일 때 기존 동작 유지
- [ ] API 파라미터 추가
