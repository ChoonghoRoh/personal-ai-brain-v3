# Phase 19 브라우저 테스트 계획서 및 결과

> **작성일**: 2026-02-22
> **1차 실행**: 2026-02-22 11:40~11:50 KST
> **2차 실행 (Docker 재시작 후 재검증)**: 2026-02-22 14:40~14:55 KST
> **대상**: Phase 19 Chain (19-1 → 19-3 → 19-4 → 19-2) 전체 기능
> **환경**: http://localhost:8001 (Docker Compose, pab-backend-ver3)
> **도구**: Claude Code + Chrome 브라우저 자동화 (MCP)

---

## 1. 테스트 범위

### 1.1 Phase 19-1: 키워드 추천 엔진 고도화
| TC | 테스트 항목 | 대상 URL | 검증 내용 | 결과 |
|----|-----------|----------|----------|------|
| TC-1-1 | 키워드 그룹 페이지 접근 | `/admin/groups` | 페이지 정상 로드, 콘솔 에러 없음 | PASS |
| TC-1-2 | 트리에서 그룹 선택 → 키워드 추천 | `/admin/groups` | 그룹 선택 후 AI 추천 버튼 동작 | **FAIL** |
| TC-1-3 | 추천 결과 키워드 표시 | `/admin/groups` | 쉼표 분리 정상, 특수문자 유지, garbled 없음 | **FAIL** |

### 1.2 Phase 19-3: 키워드 그룹 관리 UI 리뉴얼
| TC | 테스트 항목 | 대상 URL | 검증 내용 | 결과 |
|----|-----------|----------|----------|------|
| TC-3-1 | 폴더형 트리 렌더링 | `/admin/groups` | 📁/📂 아이콘 트리 정상 표시 | PASS |
| TC-3-2 | 트리 노드 클릭 피드백 | `/admin/groups` | 클릭 시 .selected 즉시 적용, 스피너 표시 | PASS |
| TC-3-3 | 우클릭 컨텍스트 메뉴 | `/admin/groups` | 노드 우클릭 시 메뉴 표시 (부모 변경 등) | PASS |
| TC-3-4 | 인라인 편집 모드 | `/admin/groups` | 그룹 선택 즉시 편집 폼 표시 (2단계 진입 아님) | PASS |
| TC-3-5 | 트리 헤더 검색 통합 | `/admin/groups` | 트리 헤더 내 검색바 + 깊이 필터 존재 | PASS |
| TC-3-6 | CSS 분리 확인 | `/admin/groups` | 스타일 깨짐 없음, 5개 CSS 파일 로드 | PASS |

### 1.3 Phase 19-4: 청크 관리 통합 1단계 (탭 기반)
| TC | 테스트 항목 | 대상 URL | 검증 내용 | 1차 결과 | 2차 결과 |
|----|-----------|----------|----------|:------:|:------:|
| TC-4-1 | 통합 페이지 접근 | `/admin/knowledge-workflow` | 페이지 정상 로드, 탭 UI 표시 | FAIL | **PASS** |
| TC-4-2 | 탭 전환 (생성/승인/관리) | `/admin/knowledge-workflow` | 3개 탭 클릭 전환 + URL ?tab= 동기화 | FAIL | **PASS** |
| TC-4-3 | 생성 탭 기능 | `?tab=create` | 파일 목록, 3단계 생성 흐름 UI 확인 | FAIL | **PASS*** |
| TC-4-4 | 승인 탭 기능 | `?tab=approval` | 대기 목록, 체크박스, 일괄 승인 UI | FAIL | **PASS** |
| TC-4-5 | 관리 탭 기능 | `?tab=manage` | 청크 목록, 라벨 관리, 다중 선택 UI | FAIL | **PASS** |
| TC-4-6 | 리다이렉트: chunk-create | `/admin/chunk-create` | → `/admin/knowledge-workflow?tab=create` | FAIL | **PASS** |
| TC-4-7 | 리다이렉트: approval | `/admin/approval` | → `/admin/knowledge-workflow?tab=approval` | FAIL | **PASS** |
| TC-4-8 | 리다이렉트: chunk-labels | `/admin/chunk-labels` | → `/admin/knowledge-workflow?tab=manage` | FAIL | **PASS** |
| TC-4-9 | LNB 메뉴 확인 | any admin page | "지식 워크플로우" 메뉴 1개 표시 | FAIL | **FAIL** |

> *TC-4-3 PASS*: 기능 자체는 정상이나 ISS-7(초기 탭 activate 미호출)로 인해 첫 로드 시 파일 목록 자동 로드 안 됨

### 1.4 Phase 19-2: 통계 메뉴 레이아웃 변경
| TC | 테스트 항목 | 대상 URL | 검증 내용 | 결과 |
|----|-----------|----------|----------|------|
| TC-2-1 | 통계 페이지 접근 | `/admin/statistics` | 페이지 정상 로드, CSS Grid 레이아웃 | PASS |
| TC-2-2 | Desktop 레이아웃 (1200px+) | `/admin/statistics` | 요약 3단, 분포 3단+트렌드, 상세 2단 | PASS |
| TC-2-3 | Tablet 레이아웃 (768~1200px) | `/admin/statistics` | 요약 2단, 분포 2단, 상세 2단 | **PARTIAL** |
| TC-2-4 | Mobile 레이아웃 (~768px) | `/admin/statistics` | 요약 1단, 분포 1단, 상세 1단 | **PARTIAL** |
| TC-2-5 | Chart.js 차트 렌더링 | `/admin/statistics` | 트렌드 차트 + 분포 차트 정상 표시 | PASS |

---

## 2. 테스트 실행 전략

### 2.1 실행 순서
1. **Phase 19-3 + 19-1** (키워드 그룹): `/admin/groups` 한 페이지에서 통합 테스트
2. **Phase 19-4** (청크 통합): `/admin/knowledge-workflow` + 리다이렉트 테스트
3. **Phase 19-2** (통계): `/admin/statistics` + 반응형 테스트

### 2.2 검증 방법
- **시각적 검증**: 스크린샷 + 페이지 구조 확인
- **기능 검증**: 클릭/입력 인터랙션 테스트
- **콘솔 에러**: `read_console_messages`로 JS 에러 확인
- **네트워크 요청**: API 호출 정상 확인
- **반응형**: `resize_window`로 3단계 해상도 테스트

---

## 3. 테스트 실행 결과

### 3.1 Phase 19-3 + 19-1: 키워드 그룹 (`/admin/groups`)

#### TC-1-1 + TC-3-1: 페이지 접근 + 폴더형 트리
- **결과**: PASS
- **확인사항**: 페이지 정상 로드, 📁 폴더 아이콘 + ▶ 화살표 트리 구조 표시, 3개 루트 그룹 렌더링
- **콘솔 에러**: 페이지 로드 시 JS 에러 없음

#### TC-3-2: 트리 노드 클릭 피드백
- **결과**: PASS
- **확인사항**: "고양이 카테고리" 노드 클릭 시 `.selected` 배경색 즉시 적용, 우측 패널에 상세 정보 로드

#### TC-3-3: 우클릭 컨텍스트 메뉴
- **결과**: PASS
- **확인사항**: "AI 개발 관련 키워드 그룹" 노드 우클릭 시 커스텀 컨텍스트 메뉴 표시 — "부모 변경", "하위 그룹 추가" 2개 항목

#### TC-3-4: 인라인 편집 모드
- **결과**: PASS
- **확인사항**: 그룹 선택 즉시 우측 패널에 인라인 편집 폼(그룹 이름, 설명, AI 키워드 추천, LLM 모델, 색상 코드) + 취소/삭제/저장 버튼 표시. 2단계 진입 없이 바로 편집 가능

#### TC-3-5: 트리 헤더 검색 통합
- **결과**: PASS
- **확인사항**: 트리 상단에 "키워드 그룹" 라벨 + 깊이 셀렉터(5) + 검색바("검색...") 컴팩트 배치

#### TC-3-6: CSS 분리 확인
- **결과**: PASS
- **확인사항**: 네트워크 요청에서 5개 CSS 파일 모두 200 OK 확인
  - `admin-groups-base.css`
  - `admin-groups-tree.css`
  - `admin-groups-panel.css`
  - `admin-groups-keywords.css`
  - `admin-groups-responsive.css`
- 스타일 깨짐 없음

#### TC-1-2 + TC-1-3: 키워드 추천 동작
- **결과**: **FAIL** (Critical)
- **증상**: "AI 키워드 추천" 버튼 클릭 시 JS 에러 발생, 추천 결과 미표시
- **에러 내용**:
  ```
  TypeError: Cannot set properties of null (setting 'disabled')
    at KeywordGroupSuggestion.suggestKeywordsFromDescription
    (keyword-group-suggestion.js:55:17)
  ```
- **원인 추정**: 19-3에서 인라인 편집 폼으로 UI 전환 시, `keyword-group-suggestion.js`가 참조하는 DOM 요소(버튼의 disabled 속성 제어 대상)가 새 폼 구조에서 누락됨
- **영향도**: 키워드 추천 핵심 기능 완전 불능

---

### 3.2 Phase 19-4: 청크 관리 통합 (`/admin/knowledge-workflow`)

> **1차 테스트**: Docker 컨테이너가 uvicorn `--reload` 없이 29시간 운영 → 구 코드 캐시로 전체 FAIL
> **근본 원인**: `docker restart pab-backend-ver3` 후 전면 재검증
> **2차 테스트**: Docker 재시작 후 14:40~14:55 KST

#### TC-4-1: 통합 페이지 접근
- **1차 결과**: **FAIL** — 404 Not Found
- **2차 결과**: **PASS**
- **확인사항**: 페이지 정상 로드, "청크 생성 → 승인 → 관리 통합" 헤더, 탭 UI("1. 생성" / "2. 승인" / "3. 관리") 표시
- **근본 원인**: uvicorn `--reload` 미설정 → 구 코드 캐시. Docker 재시작으로 해결

#### TC-4-2: 탭 전환 (생성/승인/관리)
- **2차 결과**: **PASS**
- **확인사항**:
  - "1. 생성" 클릭 → `?tab=create` + 파일 선택 UI
  - "2. 승인" 클릭 → `?tab=approval` + 청크 승인 센터 (대기 중/승인됨/거절됨 필터, 전체 승인/전체 승인+라벨 버튼)
  - "3. 관리" 클릭 → `?tab=manage` + 청크 목록 (검색바, 체크박스 다중선택)
  - URL 쿼리 파라미터 `?tab=` 자동 동기화 확인

#### TC-4-3: 생성 탭 기능
- **2차 결과**: **PASS*** (ISS-7 주의)
- **확인사항**: 수동 `activate()` 호출 후 `/api/documents` API 정상 호출 → 파일 목록(brain/, docs/ 내 .md 파일) 정상 표시, 3단계 UI(파일 선택/분할 선택/등록) 확인
- **ISS-7**: 초기 탭의 `activate()`가 자동 호출되지 않아 페이지 첫 로드 시 "파일 목록을 불러오는 중..." 상태에 멈춤

#### TC-4-4: 승인 탭 기능
- **2차 결과**: **PASS**
- **확인사항**: 승인 대기 청크 목록 정상 표시, 각 청크에 ✅승인/❌거절/상세 보기 버튼, 체크박스 다중선택, "전체 승인" + "전체 승인+라벨" 일괄 처리 버튼

#### TC-4-5: 관리 탭 기능
- **2차 결과**: **PASS**
- **확인사항**: 전체 청크 목록(ID 역순), 검색바, 체크박스 다중선택, 하단 "선택된 청크" 패널(라벨 선택/유사선택/해제/추가, 라벨 분류 필터, 청크 내용 미리보기, AI 키워드·라벨 추천)

#### TC-4-6: 리다이렉트 chunk-create
- **2차 결과**: **PASS**
- **확인사항**: `/admin/chunk-create` → 302 → `/admin/knowledge-workflow?tab=create`

#### TC-4-7: 리다이렉트 approval
- **2차 결과**: **PASS**
- **확인사항**: `/admin/approval` → 302 → `/admin/knowledge-workflow?tab=approval`

#### TC-4-8: 리다이렉트 chunk-labels
- **2차 결과**: **PASS**
- **확인사항**: `/admin/chunk-labels` → 302 → `/admin/knowledge-workflow?tab=manage`

#### TC-4-9: LNB 메뉴 확인
- **2차 결과**: **FAIL**
- **확인사항**: LNB에 여전히 기존 3개 메뉴(청크 생성, 청크 승인, 청크 관리) 표시. "지식 워크플로우" 통합 메뉴 미반영
- **원인**: `header-component.js`에 "지식 워크플로우" 메뉴가 추가되었으나, 기존 3개 메뉴가 삭제되지 않았거나 캐시 문제

---

### 3.3 Phase 19-2: 통계 레이아웃 (`/admin/statistics`)

#### TC-2-1: 페이지 접근 + CSS Grid
- **결과**: PASS
- **확인사항**: 페이지 정상 로드, CSS Grid 기반 레이아웃, 콘솔 에러 없음
- **구성요소**: 요약 카드 3개(총 문서 821, 총 청크 2,168, 총 라벨 2,809) + 분포 차트 3개 + 일별 트렌드 + 상세 현황 2단

#### TC-2-2: Desktop 레이아웃 (1568px)
- **결과**: PASS
- **확인사항**:
  - 요약: 3단 카드 1행
  - 분포: 3개 차트(문서 유형/청크 상태/라벨 유형) + 일별 트렌드 = 4단 1행
  - 상세: "인기 라벨 TOP 10" | "프로젝트별 현황" 2단 좌우 배치

#### TC-2-3: Tablet 레이아웃 (1000px)
- **결과**: **PARTIAL**
- **확인사항**:
  - 분포: 2단 ✅
  - 상세: 2단 좌우 ✅
  - **이슈**: 요약 카드(`.summary-cards`)가 x=992px에 위치 — 뷰포트(1000px) 밖으로 밀려남
  - **근본 원인**: `.admin-container`의 `grid-template-columns: 720px 742px` = 총 1462px → 780px 컨테이너 오버플로우. 반응형 breakpoint에서 grid 레이아웃 미변경

#### TC-2-4: Mobile 레이아웃 (480px)
- **결과**: **PARTIAL**
- **확인사항**:
  - 분포: 1단 상하 ✅
  - 상세: 1단 상하 ✅
  - **이슈**: 요약 카드가 우측으로 넘침(overflow) — 숫자와 텍스트가 잘려서 표시

#### TC-2-5: Chart.js 차트 렌더링
- **결과**: PASS
- **확인사항**: 3종 차트 정상 렌더링
  - 문서 유형 분포: 도넛 차트 (MD)
  - 청크 상태 분포: 바 차트 (Approved/Draft)
  - 라벨 유형 분포: 도넛 차트 (Role/Domain/Keyword_group/Keyword)
  - 일별 트렌드: 라인 차트 (문서/청크/추론, 최근 7일)

---

## 4. 종합 판정 (2차 재검증 반영)

| Phase | 테스트 수 | PASS | FAIL | PARTIAL | 판정 |
|-------|:--------:|:----:|:----:|:-------:|:----:|
| 19-1 | 3 | 1 | 2 | 0 | **FAIL** |
| 19-3 | 6 | 6 | 0 | 0 | **PASS** |
| 19-4 | 9 | 8 | 1 | 0 | **PARTIAL** |
| 19-2 | 5 | 3 | 0 | 2 | **PARTIAL** |
| **합계** | **23** | **18** | **3** | **2** | **PARTIAL** |

### 판정 근거
- **19-1 FAIL**: AI 키워드 추천 핵심 기능 불능 (Critical JS TypeError, 코드 버그 확정)
- **19-3 PASS**: 폴더형 트리, 인라인 편집, 컨텍스트 메뉴, CSS 분리 모두 정상
- **19-4 PARTIAL**: Docker 재시작 후 8/9 TC PASS. ISS-4(LNB 통합) + ISS-7(초기 탭 activate) 미해결
- **19-2 PARTIAL**: Desktop 정상, 반응형에서 요약 카드 레이아웃 이슈 (Medium)

---

## 5. 이슈 목록

| # | Phase | TC | 심각도 | 설명 |
|---|-------|-----|:------:|------|
| ISS-1 | 19-1 | TC-1-2 | **Critical** | AI 키워드 추천 버튼 클릭 시 `TypeError: Cannot set properties of null (setting 'disabled')` at `keyword-group-suggestion.js:55`. 인라인 편집 폼 전환 후 DOM 요소 참조 깨짐. Docker 재시작 후에도 동일 → 코드 버그 확정 |
| ~~ISS-2~~ | ~~19-4~~ | ~~TC-4-1~~ | ~~Critical~~ | ~~Docker 재시작으로 해결. 원인: uvicorn `--reload` 미설정 → 구 코드 캐시~~ **RESOLVED** |
| ~~ISS-3~~ | ~~19-4~~ | ~~TC-4-6~8~~ | ~~High~~ | ~~Docker 재시작으로 해결. 리다이렉트 302 정상 동작~~ **RESOLVED** |
| ISS-4 | 19-4 | TC-4-9 | **High** | LNB에 "지식 워크플로우" 통합 메뉴 미표시, 기존 3개 메뉴 그대로. `header-component.js`에 메뉴 추가됐으나 구 메뉴 미삭제 |
| ISS-5 | 19-2 | TC-2-3 | **Medium** | Tablet(1000px)에서 요약 카드 영역 미표시 — `.admin-container` grid 고정값 1462px > 컨테이너 780px |
| ISS-6 | 19-2 | TC-2-4 | **Medium** | Mobile(480px)에서 요약 카드 우측 overflow — 카드 너비가 뷰포트 초과 |
| ISS-7 | 19-4 | TC-4-3 | **High** | 초기 탭 `activate()` 미호출 — `knowledge-workflow.js` 생성자에서 `syncFromUrl()` → `switchTab("create")` 호출 시 `this.currentTab === "create"` early return. 또한 `registerTab()`이 `syncFromUrl()` 이후 실행되어 탭 등록 시점에 `activate()` 미호출. 결과: 첫 로드 시 파일 목록 영구 로딩 상태 |

---

## 6. 권장 조치

### P0 (즉시 수정 — Phase 20 선행)
1. **ISS-1**: `keyword-group-suggestion.js:55` — 인라인 편집 폼에서 추천 버튼의 disabled 제어 대상 요소 ID/선택자 수정. 19-3 인라인 편집 전환 시 DOM 구조 변경 반영 필요
2. **ISS-7**: `knowledge-workflow.js` — 초기 탭 `activate()` 호출 누락 수정. `registerTab()` 내에서 현재 탭과 일치 시 `activate()` 호출하거나, 모든 탭 등록 후 `syncFromUrl()` 재호출

### P1 (후속 수정)
3. **ISS-4**: `header-component.js` — LNB에서 기존 3개 청크 메뉴 삭제, "지식 워크플로우" 단일 메뉴로 통합
4. **ISS-5~6**: `statistics.css` 반응형 미디어쿼리 — `.admin-container` grid를 `@media (max-width: 1200px)`에서 1열로 변경

### 운영 권장
5. **Docker**: `docker-compose.yml`에 `--reload` 플래그 추가하여 개발 시 코드 변경 자동 반영

---

## 7. 추가 검증 항목 (TC 외)

### 트리 펼치기/접기
- **결과**: PASS
- **확인사항**: "고양이 카테고리" ▶ 클릭 → ▼ 전환 + 하위 노드(고양이 사료📁, 고양이 용품, 고양이 품종) 정상 표시

### 트리 검색 (타입어헤드)
- **결과**: PASS
- **확인사항**: 검색바에 "AI" 입력 → "AI 개발 관련 키워드 그룹" 자동 펼침 + "AI 그래픽 카드 종류" 노드 탐색 선택. 필터링 방식이 아닌 타입어헤드 탐색 방식으로 동작

### 깊이 필터
- **결과**: PASS
- **확인사항**: 깊이 셀렉터 옵션 3/5/전체(10). 5→3 변경 시 트리 재렌더링 정상

### TC-4-7 개별 확인
- **결과**: **FAIL** — `/admin/approval` → "청크 승인 센터" 기존 페이지 로드, 리다이렉트 없음

### TC-4-8 개별 확인
- **결과**: **FAIL** — `/admin/chunk-labels` → "청크 관리" 기존 페이지 로드, 리다이렉트 없음

---

### 2차 재검증 (Docker 재시작 후 14:40~14:55 KST)

#### 근본 원인
- Docker 컨테이너 `pab-backend-ver3`가 uvicorn `--reload` 없이 29시간 운영
- 코드는 볼륨 마운트로 최신이나, uvicorn이 구 코드 캐시 사용
- `docker restart pab-backend-ver3` 실행 → curl로 확인: `/admin/chunk-create` → 302, `/admin/knowledge-workflow` → 200

#### TC-4-1~TC-4-8: 재검증 결과
- TC-4-1 (통합 페이지): **PASS** — 탭 UI + 3탭 정상
- TC-4-2 (탭 전환): **PASS** — create/approval/manage 전환 + URL 동기화
- TC-4-3 (생성 탭): **PASS*** — `activate()` 수동 호출 후 `/api/documents` → 파일 목록 정상 (ISS-7)
- TC-4-4 (승인 탭): **PASS** — 대기/승인/거절 필터, 전체 승인 버튼
- TC-4-5 (관리 탭): **PASS** — 청크 목록, 다중선택, 라벨 패널, AI 추천 버튼
- TC-4-6~4-8 (리다이렉트): **PASS** — 3개 모두 302 정상 리다이렉트
- TC-4-9 (LNB): **FAIL** — 구 메뉴 3개 그대로 (ISS-4)

#### ISS-7 발견: 초기 탭 activate() 미호출
- `knowledge-workflow.js` 생성자에서 `this.currentTab = "create"` 초기화
- `syncFromUrl()` → `switchTab("create")` 호출 시 line 56 `if (this.currentTab === tabName) return;` early return
- `registerTab()`은 `init()` 만 호출, `activate()` 미호출
- 결과: 첫 로드 시 create 탭의 `loadStorageFileList()` 미실행 → "파일 목록을 불러오는 중..." 영구 대기
- 검증: `window.workflow.getTab('create').activate()` 수동 호출 → 파일 목록 즉시 로드

#### TC-1-2: AI 키워드 추천 재확인
- **결과**: **FAIL** — Docker 재시작 후에도 동일 TypeError 발생
- 코드 레벨 버그 확정 (ISS-1)

### ISS-5 근본 원인 분석
- `.admin-container` CSS Grid 구조: `grid-template-columns: 720px 742px` (고정값)
- 총 1462px > 컨테이너 780px (1000px - 사이드바 220px)
- `.summary-cards` 실제 좌표: `x=992, width=742` → 뷰포트 밖
- **수정 방향**: `@media (max-width: 1200px)` breakpoint에서 `.admin-container`의 grid를 1열로 변경

---

**문서 관리**: Phase 19 브라우저 테스트, 작성일 2026-02-22, 2차 재검증 완료 (Docker 재시작 후 전면 재검증 반영)

### 미해결 이슈 요약 (Phase 20 편성 대상)
| # | 심각도 | 설명 | 수정 대상 파일 |
|---|:------:|------|--------------|
| ISS-1 | Critical | AI 키워드 추천 TypeError | `keyword-group-suggestion.js:55` |
| ISS-7 | High | 초기 탭 activate() 미호출 | `knowledge-workflow.js` (생성자/registerTab) |
| ISS-4 | High | LNB 구 메뉴 미삭제 | `header-component.js` |
| ISS-5 | Medium | Tablet 요약 카드 오버플로우 | `statistics.css` (반응형 grid) |
| ISS-6 | Medium | Mobile 요약 카드 오버플로우 | `statistics.css` (반응형 grid) |
