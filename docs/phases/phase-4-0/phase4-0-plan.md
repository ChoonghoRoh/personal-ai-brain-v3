# Phase 4.0: Personal AI Brain – 사용성 강화 실행 문서

본 문서는 Phase 4의 목표를 **실행 가능한 단위**로 구체화하고,
순차적으로 진행 가능한 구조로 설계한다.

---

# 1️⃣ Phase 4 – 실행 가능한 Step-by-Step 실행 계획

## 🎯 Phase 4 목표 요약

- CLI 중심 환경 → 웹 기반 사용자 환경으로 확장
- 검색 / 문서 / 로그 / 시스템 상태를 한눈에 보는 환경 제공
- 사용자 경험 중심 UX 강화

---

## 🚀 Phase 4 전체 실행 흐름

### 4.1 단계 – 최소 기능 Web UI 오픈 (MVP)

목표:
“브라우저에서 검색 + 시스템 요약을 볼 수 있는 환경 제공”

#### ✔️ 해야 할 일

- FastAPI 기본 서버 구축
- API Layer 구축

  - `/api/search`
  - `/api/system/status`

- 기본 Web UI 구축

  - `/dashboard`
  - `/search`

#### 🎯 완료 기준

- 브라우저에서 검색 가능
- 대시보드에서 상태 확인 가능

---

### 4.2 단계 – 사용자 도구 수준으로 확장

목표:
“문서 활용과 AI 질의까지 웹에서 처리 가능하게”

#### ✔️ 해야 할 일

- Document Viewer 구축

  - Markdown Viewer
  - PDF Viewer
  - DOCX Viewer

- AI Ask Panel 구축

  - `/ask` 페이지
  - GPT4All 연동

- System Panel 확장

  - 자동화 상태 표시
  - 최근 업데이트 문서 표시

#### 🎯 완료 기준

- 검색 → 문서 확인 → AI 질의까지 웹에서 가능

---

### 4.3 단계 – 전문가 도구 수준으로 고도화

목표:
“관리 + 분석까지 가능한 전문가용 시스템으로 진화”

#### ✔️ 해야 할 일

- Log Viewer 구축
- 검색 UX 고도화

  - 하이라이팅
  - 추천 문서
  - 검색 히스토리

- Dashboard 고도화

  - 통계 패널
  - 활동 분석

#### 🎯 완료 기준

- 관리 + 분석까지 가능한 완전한 Web Ops 환경 완성

---

---

# 2️⃣ FastAPI + React 구조 상세 설계

---

## 🧩 전체 구조 개념

```
[Frontend: React]
 ├── UI Pages
 ├── Dashboard
 ├── Search UI
 ├── Document Viewer
 └── AI Chat Panel
        ↓ REST API
[Backend: FastAPI]
 ├── Search API
 ├── Document API
 ├── AI API
 ├── System API
 └── Log API
        ↓
[Core System]
 ├── Qdrant
 ├── GPT4All
 ├── brain/ docs
 ├── watcher / auto_commit
 └── system logs
```

---

## ✔️ Backend – FastAPI 설계

### 📌 기본 구조

```
backend/
 ├── main.py
 ├── routers/
 │   ├── search.py
 │   ├── documents.py
 │   ├── system.py
 │   └── logs.py
 ├── services/
 └── models/
```

---

### 🔎 Search API

```
GET /api/search?q=keyword
```

Response

```
[
  {
    "document_id": "xxx",
    "file": "/brain/projects/..",
    "score": 0.92,
    "snippet": "내용 일부..."
  }
]
```

---

### 📂 Document API

```
GET /api/documents
GET /api/document/{id}
```

---

### 🧠 AI API

```
POST /api/ask
{
  "question": "...",
  "context_enabled": true
}
```

---

### 🩺 System API

```
GET /api/system/status
```

---

### 📝 Logs API

```
GET /api/logs
```

---

## ✔️ Frontend – React 설계

### 📌 기본 구조

```
web/
 ├── src/
 │   ├── pages/
 │   │   ├── Dashboard.jsx
 │   │   ├── Search.jsx
 │   │   ├── Document.jsx
 │   │   ├── Ask.jsx
 │   │   └── Logs.jsx
 │   ├── components/
 │   ├── api/
 │   └── styles/
 └── public/
```

---

### 🖥️ Dashboard

- 시스템 상태 표시
- 최근 작업
- 문서 통계
- 자동화 상태

---

### 🔎 Search Page

- 검색 입력
- 결과 리스트
- 문서 미리보기
- “AI에게 물어보기” 버튼

---

### 📄 Document Viewer

- Markdown → HTML
- PDF Embed
- DOCX 변환 View
- 관련 문서 추천

---

### 🤖 Ask Page

- Chat UI
- Qdrant + GPT4All 연동
- 대화 기록 선택 저장

---

### 🧾 Logs Page

- work_log.md 표시
- timeline view
- 검색 가능

---

---

# 3️⃣ Phase 4 용 디렉토리 구조 설계

---

## 📁 최종 구조

```
personal-ai-brain/
├── brain/
├── scripts/
├── backend/          # FastAPI Backend
│   ├── main.py
│   ├── routers/
│   ├── services/
│   ├── models/
│   └── config.py
├── web/              # React Frontend
│   ├── src/
│   ├── public/
│   └── package.json
├── docs/
│   ├── phase4-usability-plan.md
│   ├── phase4-usability-execution.md (본 문서)
│   └── ui-guide.md
└── qdrant-data/
```

---

# ✅ Phase 4 최종 완료 기준

| 영역   | 완료 기준                                            |
| ------ | ---------------------------------------------------- |
| UI     | Dashboard + Search + Document + Ask + Logs 사용 가능 |
| API    | Search / System / Document / AI / Log API 완성       |
| UX     | 검색 → 문서 → 이해 → 기록까지 1 Flow 완성            |
| 운영성 | 시스템 상태 확인 + 로그 관리 가능                    |

---

# 완료 후 기대 효과

✔️ CLI 의존도 제거
✔️ 누구나 쉽게 사용할 수 있는 UI 제공
✔️ 시스템 상태 가시성 확보
✔️ AI 브레인이 “사용되는 시스템”으로 진화
