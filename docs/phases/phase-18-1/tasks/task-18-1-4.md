# Task 18-1-4: 연관 키워드 섹션 UI

- **우선순위**: P1
- **의존성**: 18-1-5 (API 필요)
- **담당 팀원**: frontend-dev
- **상태**: PENDING

## §1 목적

3단(키워드 목록) 하단에 "연관 키워드 추천" 섹션을 추가하여 유사도 기반 키워드를 조회·추가할 수 있게 한다.

## §2 변경 범위

| 파일 | 변경 유형 |
|------|----------|
| `web/src/pages/admin/groups.html` | 연관 키워드 섹션 HTML 추가 |
| `web/public/js/admin/keyword-group-matching.js` 또는 신규 | 연관 키워드 조회/추가 로직 |
| `web/public/css/admin/admin-groups.css` | 연관 키워드 스타일 |

## §3 구현 상세

1. `#keywords-list` 하단에 연관 키워드 섹션 추가
2. 검색 입력 + [조회] 버튼: `GET /api/labels/groups/{group_id}/related-keywords?q={query}`
3. 결과: 키워드명 + 유사도 % 배지 + 체크박스
4. [추가] 버튼: 선택된 키워드를 현재 그룹에 추가 (POST /api/labels/groups/{group_id}/keywords)

## §4 완료 기준

- [ ] 연관 키워드 섹션 표시
- [ ] 검색 + 조회 동작
- [ ] 유사도 % 표시
- [ ] 선택 → 그룹 추가 동작
