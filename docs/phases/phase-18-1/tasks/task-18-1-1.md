# Task 18-1-1: 그룹 관리 페이지 탭 UI

- **우선순위**: P0
- **의존성**: 없음
- **담당 팀원**: frontend-dev
- **상태**: PENDING

## §1 목적

1단 컬럼(그룹 목록)의 목록/트리 토글 버튼을 탭 UI로 변환하여 목록 탭과 트리 연결 탭으로 명확히 분리한다.

## §2 변경 범위

| 파일 | 변경 유형 |
|------|----------|
| `web/src/pages/admin/groups.html` | 탭 HTML 구조 변경 |
| `web/public/js/admin/keyword-group-manager.js` | switchView → 탭 전환 로직 |
| `web/public/css/admin/admin-groups.css` | 탭 스타일 추가 |

## §3 구현 상세

1. `.col-header` 내 `.view-toggle` 버튼 → `.tab-header` 탭 UI로 변경
2. 탭 HTML: `<div class="tab-header"><button class="tab active" data-tab="list">목록</button><button class="tab" data-tab="tree">트리 연결</button></div>`
3. 탭 전환 시 `#groups-list`/`#groups-pagination`과 `#groups-tree` 표시/숨김
4. 기존 `switchView()` 함수를 탭 전환에 맞게 수정

## §4 완료 기준

- [ ] 목록 탭 / 트리 연결 탭 전환 동작
- [ ] 목록 탭: 기존 그룹 목록 + 페이지네이션 표시
- [ ] 트리 연결 탭: 기존 트리뷰 표시
- [ ] 콘솔 에러 없음
