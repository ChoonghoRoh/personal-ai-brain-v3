# Task 18-1-3: 키워드 목록 상단에 LLM 추천 버튼 복구

- **우선순위**: P1
- **의존성**: 없음
- **담당 팀원**: frontend-dev
- **상태**: PENDING

## §1 목적

3단(키워드 목록) col-header에 [LLM 추천] 버튼을 추가하고, 클릭 시 현재 선택된 그룹의 description으로 키워드 추천 API를 호출한다.

## §2 변경 범위

| 파일 | 변경 유형 |
|------|----------|
| `web/src/pages/admin/groups.html` | 3단 col-header에 버튼 추가 |
| `web/public/js/admin/keyword-group-suggestion.js` | 추천 결과 키워드 목록 하단에 표시 |

## §3 구현 상세

1. 3단 `.col-header`에 `<button class="btn btn-sm btn-ai" onclick="suggestKeywordsFromDescription()">🤖 LLM 추천</button>` 추가
2. 그룹 미선택 시 비활성화 (disabled)
3. 추천 결과를 `#keywords-list` 하단 섹션에 표시
4. 기존 `KeywordGroupSuggestion` 클래스 활용 (API: POST /api/labels/groups/suggest-keywords)

## §4 완료 기준

- [ ] LLM 추천 버튼 표시
- [ ] 그룹 선택 후 클릭 시 추천 결과 표시
- [ ] 추천 키워드 선택 후 그룹에 추가 가능
