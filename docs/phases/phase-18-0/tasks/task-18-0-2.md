# Task 18-0-2: labels_tree.py 분리

- **우선순위**: P0
- **의존성**: 18-0-1
- **담당 팀원**: backend-dev
- **상태**: PENDING

## §1 목적

`labels_handlers.py`에서 트리 관련 핸들러 함수들을 `labels_tree.py`로 추출한다.

## §2 변경 범위

| 파일 | 변경 유형 |
|------|----------|
| `backend/routers/knowledge/labels_tree.py` | 신규 생성 |
| `backend/routers/knowledge/labels_handlers.py` | 트리 함수 제거 |
| `backend/routers/knowledge/labels.py` | import 경로 변경 |

## §3 구현 상세

1. `labels_handlers.py`에서 트리 관련 함수 식별 (tree 조회, move, breadcrumb)
2. `labels_tree.py` 파일 생성
3. 트리 함수들과 필요한 import를 이동
4. `labels.py` 라우터에서 import 경로 변경

## §4 완료 기준

- [ ] labels_tree.py에 트리 핸들러 존재
- [ ] labels_handlers.py에서 트리 핸들러 제거
- [ ] labels.py의 import 경로 업데이트
- [ ] 서버 import 에러 없음
