# Task 18-0-3: labels_suggest.py 분리 + 마무리

- **우선순위**: P0
- **의존성**: 18-0-2
- **담당 팀원**: backend-dev
- **상태**: PENDING

## §1 목적

`labels_handlers.py`에서 AI 추천 핸들러를 `labels_suggest.py`로 추출하고, 원본 파일을 삭제하여 리팩토링을 완료한다.

## §2 변경 범위

| 파일 | 변경 유형 |
|------|----------|
| `backend/routers/knowledge/labels_suggest.py` | 신규 생성 |
| `backend/routers/knowledge/labels_handlers.py` | 삭제 |
| `backend/routers/knowledge/labels.py` | import 경로 최종 정리 |

## §3 구현 상세

1. `labels_handlers.py`에서 추천 관련 함수 식별 (suggest-parent, suggest-keywords)
2. `labels_suggest.py` 파일 생성
3. 추천 함수들과 필요한 import를 이동
4. 원본 `labels_handlers.py` 삭제
5. Grep으로 프로젝트 전체에서 `labels_handlers` 참조 확인 및 수정
6. pytest 전체 실행 → 통과 확인
7. 리팩토링 레지스트리 해소 이력 기재

## §4 완료 기준

- [ ] labels_suggest.py에 추천 핸들러 존재
- [ ] labels_handlers.py 완전 삭제
- [ ] 프로젝트 전체에서 labels_handlers 참조 0건
- [ ] pytest 전체 통과
- [ ] 리팩토링 레지스트리 업데이트
